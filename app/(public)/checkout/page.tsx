import { PlaceholderPage } from "@/components/placeholder-page";

export default function CheckoutPage() {
  return (
    <PlaceholderPage
      title="Checkout"
      description="Collect selected doctor, optional referral code, and create Stripe checkout session."
    />
  );
}
